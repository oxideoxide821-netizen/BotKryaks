-- Уведомление при запуске
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "Doorkeeper Exploit",
    Text = "Интерфейс загружен. Нажми кнопку для старта.",
    Duration = 5
})

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

-- Создаём ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "DoorkeeperUI"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = game:GetService("CoreGui")

-- Основной фрейм кнопки (чёрный фон с красной рамкой)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 220, 0, 60)
mainFrame.Position = UDim2.new(0, 20, 0.5, -30)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
mainFrame.BorderColor3 = Color3.fromRGB(180, 0, 0)
mainFrame.BorderSizePixel = 3
mainFrame.Parent = screenGui

-- Угловая рамка для эффекта "вдавленности"
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 4)
corner.Parent = mainFrame

-- Внутренняя тень (эффект глубины)
local shadow = Instance.new("Frame")
shadow.Name = "Shadow"
shadow.Size = UDim2.new(1, 0, 1, 0)
shadow.Position = UDim2.new(0, 2, 0, 2)
shadow.BackgroundColor3 = Color3.fromRGB(60, 0, 0)
shadow.BorderSizePixel = 0
shadow.ZIndex = -1
shadow.Parent = mainFrame

local shadowCorner = Instance.new("UICorner")
shadowCorner.CornerRadius = UDim.new(0, 4)
shadowCorner.Parent = shadow

-- Кнопка
local button = Instance.new("TextButton")
button.Name = "ToggleButton"
button.Size = UDim2.new(1, -6, 1, -6)
button.Position = UDim2.new(0, 3, 0, 3)
button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
button.BorderColor3 = Color3.fromRGB(220, 0, 0)
button.BorderSizePixel = 2
button.Text = "▶ АУРА: ВЫКЛ"
button.TextColor3 = Color3.fromRGB(200, 200, 200)
button.TextSize = 16
button.Font = Enum.Font.Code -- Моноширинный шрифт для ретро-вайба
button.Parent = mainFrame

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 2)
buttonCorner.Parent = button

-- Статус-индикатор (маленькая точка слева от текста)
local statusDot = Instance.new("Frame")
statusDot.Name = "StatusDot"
statusDot.Size = UDim2.new(0, 8, 0, 8)
statusDot.Position = UDim2.new(0, 10, 0.5, -4)
statusDot.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
statusDot.BorderSizePixel = 0
statusDot.Parent = button

local dotCorner = Instance.new("UICorner")
dotCorner.CornerRadius = UDim.new(1, 0)
dotCorner.Parent = statusDot

-- Метка для перетаскивания (верхняя полоска)
local dragHandle = Instance.new("Frame")
dragHandle.Name = "DragHandle"
dragHandle.Size = UDim2.new(1, 0, 0, 4)
dragHandle.Position = UDim2.new(0, 0, 0, 0)
dragHandle.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
dragHandle.BorderSizePixel = 0
dragHandle.Parent = mainFrame

-- Перетаскивание
local dragging = false
local dragStart, startPos

dragHandle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

game:GetService("UserInputService").InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- Логика ауры
local auraActive = false
local auraConnection

local function startAura()
    auraActive = true
    button.Text = "■ АУРА: ВКЛ"
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    statusDot.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    
    -- Эффект "включено" — рамка ярче
    mainFrame.BorderColor3 = Color3.fromRGB(255, 0, 0)
    
    auraConnection = task.spawn(function()
        while auraActive do
            local slapEvent = game.ReplicatedStorage:FindFirstChild("GeneralHit", true) 
                or game.ReplicatedStorage:FindFirstChild("Slap", true)
                or game.ReplicatedStorage:FindFirstChild("Hit", true)

            if slapEvent then
                for _, player in pairs(Players:GetPlayers()) do
                    if not auraActive then break end
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
                        if humanoid and humanoid.Health > 0 then
                            slapEvent:FireServer(player.Character.HumanoidRootPart)
                            task.wait(0.01)
                        end
                    end
                end
            end
            task.wait(0.8)
        end
    end)
end

local function stopAura()
    auraActive = false
    button.Text = "▶ АУРА: ВЫКЛ"
    button.TextColor3 = Color3.fromRGB(200, 200, 200)
    statusDot.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
    mainFrame.BorderColor3 = Color3.fromRGB(180, 0, 0)
end

-- Анимация нажатия
button.MouseButton1Down:Connect(function()
    button.BackgroundColor3 = Color3.fromRGB(60, 0, 0)
    button.Position = UDim2.new(0, 4, 0, 4)
end)

button.MouseButton1Up:Connect(function()
    button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    button.Position = UDim2.new(0, 3, 0, 3)
end)

button.MouseLeave:Connect(function()
    button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    button.Position = UDim2.new(0, 3, 0, 3)
end)

-- Переключение ауры
button.MouseButton1Click:Connect(function()
    if auraActive then
        stopAura()
    else
        startAura()
    end
end)

-- Hover эффект
button.MouseEnter:Connect(function()
    if not auraActive then
        button.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    end
end)

button.MouseLeave:Connect(function()
    if not auraActive then
        button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    end
end)
